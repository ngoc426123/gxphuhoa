mixin newsInner(item)
  .newsInner(class=[item.isV2 && 'v2'])
    .img
      a(href=item.link title=item.title)
        img(src=item.img alt=item.title)
    .caption
      if item.cat
        .cat!=item.cat
      .tend
        if item.date
          .date!=item.date
        !=item.title